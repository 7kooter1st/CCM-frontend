# Скопируйте в .env.gh-pages и подставьте свой URL бэкенда.
# Для GitHub Pages + локальный бэкенд: бэкенд должен работать по HTTPS (см. DEPLOY_PAGES.md).
# Пример для локального бэкенда на порту 8000 с mkcert:
VITE_API_URL=https://localhost:8000
